Conduct a two-phase flow simulation of water overflowing a weir using the interFoam solver with the k-epsilon turbulence model in OpenFOAM. The system comprises water (density 1000 kg/m³, kinematic viscosity 1e-06 m²/s) and air (density 1 kg/m³, kinematic viscosity 1.48e-05 m²/s), both Newtonian fluids, with a surface tension coefficient of 0.07 N/m. Gravity is set as (0, -9.81, 0) m/s². Initially, the water volume fraction field (alpha.water) and the velocity field (U) are uniformly zero throughout the domain. The modified pressure field (p_rgh) starts at a predefined value. Turbulence fields k (turbulent kinetic energy) and epsilon (turbulent dissipation rate) begin uniformly at specified initial values, with the turbulent viscosity field (nut) initialized to zero.

Boundary conditions include a variableHeightFlowRate and variableHeightFlowRateInletVelocity for the water volume fraction and velocity at the inlet, respectively. The outlet applies a zeroGradient condition for pressure and water fraction, while the atmosphere boundary uses an inletOutlet condition for the water fraction and a pressureInletOutletVelocity for velocity. The lower wall enforces a no-slip condition for velocity, uses a fixedFluxPressure condition for pressure, and employs wall functions (epsilonWallFunction and kqRWallFunction) for turbulence quantities. Other boundaries default to empty for a 2D simulation.

Discretization employs an Euler scheme for temporal derivatives. Spatial gradients use Gauss linear interpolation. The water volume fraction divergence utilizes the Gauss vanLeer scheme to preserve sharp interfaces. Divergence terms for turbulence variables (k, epsilon) employ Gauss upwind schemes. Velocity divergence applies Gauss linear. The pressure laplacian is solved using Gauss linear corrected, while velocity components rely on smoothSolver with symGaussSeidel smoother. The PIMPLE algorithm performs pressure-velocity coupling with 3 correctors, without momentum prediction. Solvers include PCG with DIC preconditioning for pressure fields (p_rgh), and specific tolerances are set for pressure and turbulence equations.