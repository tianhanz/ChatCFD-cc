Conduct a two-dimensional steady-state potential flow simulation for the pitzDaily case using the potentialFoam solver without turbulence modeling. The flow is treated as incompressible and inviscid. For spatial discretization, employ Gauss linear schemes for gradients and corrected linear schemes for Laplacian terms. Use bounded Gauss linear for the divergence of the velocity flux (phi,U). Apply a geometric-algebraic multigrid (GAMG) solver with a diagonal incomplete Cholesky (DIC) smoother for the potential field equation, setting absolute tolerance to 1e-06 and relative tolerance to 0.01. Perform two non-orthogonal corrections. Set initial pressure uniformly to zero internally with zero gradient boundaries except at the outlet where pressure is fixed at zero. Initialize velocity uniformly to zero internally with fixed uniform inflow velocity (10 0 0) at the inlet, zero gradient at the outlet, and no-slip conditions on the walls. Apply an empty boundary condition for the frontAndBack planes.