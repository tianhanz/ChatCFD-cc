The laminar contactAngleCavity case employs the pimpleFoam solver to simulate an incompressible two-phase flow with interfacial dynamics and prescribed contact angles. The fluid is Newtonian, with a kinematic viscosity of 1e-05 m²/s, density of 1000 kg/m³, and surface tension coefficient of 0.15 N/m. Gravity is explicitly set to zero. A free surface exists at the top boundary, modeled using an interfaceTrackingFvMesh dynamic mesh. Contact angles of 70 degrees are enforced on the left and right boundaries. Initial velocity and relative pressure fields are uniformly zero.

The discretization uses a backward scheme for time derivatives. Gradients utilize linear Gauss interpolation. Divergence schemes apply Gauss linear for the momentum flux term. Laplacian terms and surface normal gradients employ corrected linear Gauss schemes. The PIMPLE algorithm uses 12 outer correctors and 3 inner velocity-pressure correctors, with no non-orthogonal correction. Pressure solvers use GAMG with Gauss-Seidel smoothing and tight tolerances. Velocity equations apply a symmetric Gauss-Seidel smoother, also with low tolerances. Relaxation factors are set to 1. The dynamic mesh motion is solved via a velocityLaplacian approach. The free surface velocity and pressure use specialized boundary conditions, while side walls utilize slip conditions.