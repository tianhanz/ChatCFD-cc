Perform a three-dimensional multiphase flow simulation using the MPPICInterFoam solver with particle interactions in a two-phase system (water and air). The physical models include turbulence modelled via k-epsilon RAS with on coefficients, gravity, and interfacial forces. Water has a density of 1000 kg/m³ and kinematic viscosity of 1e-06 m²/s, while air has a density of 1 kg/m³ and kinematic viscosity of 1.48e-05 m²/s. The surface tension coefficient is 0.07 N/m. Particle properties include a reference density of 1200 kg/m³ with WenYu drag forces acting on particles relative to the continuous phase.

Discretization uses Euler scheme for time derivatives, Gauss linear corrected for laplacians, and Gauss vanLeer for div(phi,alpha). Other divergence terms (U, k, epsilon) use Gauss upwind. Gradients and surface normal gradients default to Gauss linear and corrected schemes. Solver settings involve PIMPLE algorithm with 3 pressure-velocity correctors without momentum predictor and no non-orthogonal corrections. The alpha.water field uses MULES with 2 sub-cycles and smoothSolver while pressure (p_rgh) is solved with PCG/DIC. Velocity, k, and epsilon fields employ smoothSolver/symGaussSeidel. Particle phase alpha employs GAMG with GaussSeidel smoothing.

Initial fields set alpha.water uniformly to 1, velocity to zero, modified pressure p_rgh to zero, k to 0.01 m²/s², epsilon to 0.1 m²/s³, and nut to zero. The inlet boundary condition for alpha.water transitions uniformly from 1 to 0 after 1 second. Velocity at inlet has a timedependent profile with evolving direction. Outlet pressure is zero with fixed total pressure. Walls and base enforce no-slip velocity, zero-gradient alpha.water, and wall functions for turbulence quantities. Particle injections occur at the inlet with defined size distribution and rebound interactions at walls/base while escape occurs at outlet/inlet.