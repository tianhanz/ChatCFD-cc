Perform a steady-state incompressible turbulent flow simulation using the simpleFoam solver for the 2D PitzDaily experimental case configuration. The k-epsilon turbulence model is employed to capture turbulent effects. Use experimental inlet profiles mapped via `timeVaryingMappedFixedValue` boundary conditions for velocity `U`, turbulent kinetic energy `k`, and turbulent dissipation rate `epsilon`. The outlet uses a zero-gradient pressure condition and fixed pressure value. Upper and lower walls apply no-slip conditions for velocity and utilize wall functions (`kqRWallFunction` for `k`, `epsilonWallFunction` for `epsilon`).

For spatial discretization, use the bounded Gauss upwind scheme for convective terms in momentum and turbulence transport equations (`div(phi,U)`, `div(phi,k)`, `div(phi,epsilon)`). Gradients are computed with Gauss linear, and Laplacian terms use Gauss linear corrected schemes. The solution employs the SIMPLE algorithm. Set pressure field relaxation to 0.3 and relaxation for velocity and turbulence equation variables (`U`, `k`, `epsilon`, `R`, `nuTilda`) to 0.7. Target solution residuals are set to 0.01 for pressure, 0.001 for velocity, and 0.001 for `k` and `epsilon`.

The fluid properties are Newtonian with a constant kinematic viscosity `nu` of 1e-05 m²/s. Initial conditions consist of uniform zero velocity and pressure fields within the domain, along with uniform initial values of `k` (0.375 m²/s²) and `epsilon` (14.855 m²/s³).